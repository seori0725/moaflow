# MoaFlow 학원비 결제 작업 인수인계

> 이 문서는 2026-08-18 작업을 기준으로 새로 정리했다. **노트북 교체가 예정되어 있어**, 새 장치에서 이어갈 때 필요한 설정 절차를 맨 앞에 둔다.

## 반드시 먼저 확인

- 저장소: `C:\Users\PCuser\Documents\edutong` (새 장치에서는 아래 "새 장치에서 이어하기" 참고)
- 원격 저장소: `https://github.com/seori0725/moaflow.git`
- Firebase 프로젝트: `moaflow-1b9c8`
- 배포 주소: `https://moaflow-1b9c8.web.app/`
- 발표/데모 주소: `https://moaflow-1b9c8.web.app/?demo=preview`
- 로컬 주소: `http://127.0.0.1:4173/` (대용량 QA: `?qa=large`)
- 데모 인증번호: `123456`
- 현재 `main`과 `codex/tuition-payments`는 **완전히 병합되어 동일한 커밋**을 가리킨다(`e20b90f`). 미커밋 작업 없음, 미푸시 커밋 없음 — 2026-08-18 기준 깨끗한 상태.
- 배포 사이트는 로컬 파일과 **바이트 단위로 동일**함을 diff로 확인했다(`payments.js`, `styles.css` 등).

## 새 장치에서 이어하기 (노트북 교체 시)

1. `git clone https://github.com/seori0725/moaflow.git` 후 `git checkout main` (또는 이어서 작업할 기능 브랜치를 새로 만든다: `git checkout -b claude/다음작업명`).
2. Node.js 설치 확인, `npm install`.
3. GitHub CLI 설치 후 `gh auth login` — 이전 장치의 토큰은 옮겨지지 않으므로 반드시 새로 인증한다.
4. Firebase CLI 설치(`npm install -g firebase-tools` 또는 `npx firebase-tools`) 후 `firebase login`, 이어서 `firebase use moaflow-1b9c8`로 프로젝트 연결 확인.
5. `npm start` → `http://127.0.0.1:4173` 확인, `npm test` / `npm run test:payments`로 기존 실패 목록이 아래 "알려진 기존 실패"와 일치하는지 확인.
6. `moaflow_brand_update/`, `tools/`는 git에 올라가 있지 않은 로컬 전용 폴더다(브랜드 이미지·문서, 파이썬 스크립트). 새 장치에는 자동으로 없다 — 필요하면 이전 장치에서 수동으로 옮긴다. 결제 작업과는 무관하니 옮기지 않아도 무방하다.
7. GitHub·Firebase 토큰은 저장소에 저장돼 있지 않다. 커밋·코드에서 토큰이 보이면 즉시 사용자에게 알린다.

## 사용자가 확정한 결제 정책

### 결제 방식

1. `매월 자동결제`
   - 학부모가 카드를 연결하고 자동결제에 동의한다.
   - 매월 납부일에 등록 카드로 자동결제한다.
   - 설정 저장만으로 즉시 결제하면 안 된다.
2. `매월 직접결제`
   - 학부모가 청구 건을 일부 선택해 카드로 결제할 수 있다. 카드는 선택한 청구 건의 금액을 정확히 청구한다(이미 가상계좌로 일부 납부된 청구 건이면 남은 잔액만 청구).
   - 가상계좌 입금은 청구 건을 선택하는 방식이 아니다. 학부모 고정 가상계좌로 들어온 입금액은 자동으로 배분된다(아래 고정형 가상계좌 항목 참고).

### 고정형 가상계좌

- 사용자가 KCP에서 계약·사용한 상품은 일반 일회성 가상계좌가 아니라 고정형 가상계좌다.
- 학원별 계좌가 아니라 `학부모 1명당 고정형 가상계좌 1개`를 발급한다. `매월 직접결제` 모드로 전환하는 순간 자동 발급된다(선택 행위 불필요).
- 해지 전까지 같은 계좌를 계속 사용한다. 자녀와 학원이 달라도 같은 학부모 계좌로 입금한다.
- 입금액은 **청구일(issuedAt)이 오래된 청구 건부터 자동으로 배분**된다. 한 청구 건 안에서 부분 납부를 지원하며(`paidAmount` 필드), 초과분은 다음 청구 건으로 이월되고 모두 채우고 남으면 `guardianVirtualAccounts[].creditBalance`에 예치되어 다음 입금과 합산된다.
- 핵심 로직: `payments.js`의 `allocateVirtualAccountDeposit(state, guardianUserId, amount, context)`. 실제 KCP 웹훅이 붙으면 이 함수를 그대로 호출하면 된다.
- **`creditBalance`(예치금)는 화면에 표시되지 않는다.** 내부 계산에만 쓰이는 값이다. 학부모 화면에는 입금 시뮬레이션 버튼도 없다(사용자 요청으로 삭제) — 검증은 `window.MoaFlowPayments.allocateVirtualAccountDeposit`을 콘솔/테스트에서 직접 호출한다.
- 현재는 실제 KCP·카드 승인을 호출하지 않는 모의 구현이다.

### 카드 등록 버튼

- `결제 설정` 폼 안, 저장 버튼 바로 앞에 있다. **자동결제·직접결제 두 모드 모두에서 노출**된다(초반에는 자동결제에서만 보이고 CSS 스코프 버그로 직접결제에서도 항상 보였는데, 이제는 의도적으로 항상 보이도록 고쳤다).
- 클릭하면 `카드 등록 페이지는 추후 제공됩니다.` 안내만 표시한다. 실제 카드 등록/저장(vault)은 아직 만들지 않았다 — 사용자가 향후 "등록 카드 있으면 자동으로 그 카드로 결제, 없으면 카드 입력창 자동 노출"을 계획 중이며 지금은 위치·노출만 구현했다.

## 오늘(2026-08-18) 작업 내역

### 결제 설정 폼 레이아웃 정리

- `styles.css`의 `.guardian-payment-setting-form`이 5열 그리드(드롭다운/설명박스 2칸/카드등록/저장)로 바뀌었다.
- 761~1180px(태블릿) 구간에서 카드등록 버튼이 `justify-self:end`에 걸려 큰 빈 여백이 생기던 버그를 고쳤다(`justify-self: stretch`로 변경, 위 드롭다운과 좌우 위치·너비 일치).
- 데스크톱에서도 "저장" 버튼만 `justify-self:end`라 카드등록과 여백이 안 맞던 문제를 고쳤다 — 이제 카드등록·저장 모두 자기 칸을 꽉 채운다(`justify-self: stretch`), 모든 칸 사이 여백이 14px로 통일됨.
- 결제방식 드롭다운(`#guardian-payment-mode`) 글자가 12px로 작게 나오던 것을 13px로 맞춰 카드등록·저장 버튼과 통일했다(원인: 감싸는 `<label>`의 `font-size:12px` 상속).
- "매월 직접결제" 뱃지 색을 회색 → 퍼플(`--purple`)로 변경(자동결제의 초록과 대비되면서도 경고색은 아님, 앱 내 "휴원" 등 비-기본 상태에 이미 쓰이는 색).

### 결제 내역 조회 UI

- "월별" 조회가 `<input type="month">`(네이티브 달력 팝업, 월/일 혼동 유발)였던 것을 `<select>` 드롭다운으로 바꿔 "연별"과 동일한 방식으로 통일했다(최근 24개월 목록, `payments.js`의 `periodControls`).

### 발표용 데모 데이터셋 (`?demo=preview`)

- `qa-data.js`에 `createPresentationDemoState`를 추가했다. **학원 2곳**, 원장 2명, 강사 1명, 학부모 1명(송지호)으로 구성된 소규모 큐레이션 데이터다. "모아플로는 여러 학원을 한 앱으로 관리하는 게 핵심"이라는 사용자 요구에 맞춰, 자녀 1명이 여러 학원에 다니는 경우까지 보여주도록 설계했다.
  - 학원1 "모아플로 데모학원"(수학, 원장 김도윤, 강사 이서연) / 학원2 "브릿지 영어 데모학원"(영어, 원장 이하늘 — 데모 로그인으로는 접근 불가, 아래 로그인 제약 참고).
  - 자녀 송이안: **두 학원 모두** 다닌다(학원1 수학1반 + 학원2 영어1반). 이번달 수학 청구서는 **부분납부**(잔액 표시), 영어 청구서는 **미납**.
  - 자녀 송민재: 학원1 수학1반 **한 곳만** 다닌다. 이번달 청구서는 아직 기한 전(정상 청구 상태).
  - 지난 2개월 청구서는 모두 완납 처리되어 있다.
  - 즉 "학부모 1명 - 자녀별로 다니는 학원 수가 다름(2개 vs 1개)"과 "학부모 기준 연결 학원 2곳" 두 가지를 한 계정에서 동시에 보여준다.
- `app.js`에 `demoMode` 트리거를 추가했다(`?demo=preview`, `qaMode`와 달리 **localhost 제한 없음** — 배포 도메인에서도 동작). 새로고침마다 깨끗한 상태로 재생성된다(persist 안 됨, `?qa=large`와 같은 방식).
- **중요한 로그인 제약**: 전화번호는 형식만 검증하고 실제 로그인은 `state.users.find(역할 일치하는 첫 사용자)`로 처리된다(`app.js`의 `completeLogin`). 즉 역할별로 사용자가 여러 명 있어도 항상 배열의 **첫 번째** 사용자로만 로그인된다. 그래서 원장 데모 로그인은 학원1의 김도윤만 접근 가능하고, 학원2 원장(이하늘)은 데이터에는 존재하지만 로그인으로는 도달할 수 없다(운영자·학부모 화면에서는 정상적으로 보인다). 여러 학부모/원장 계정을 실제로 오가며 시연하고 싶다면 로그인 로직 자체를 바꿔야 하는데, 이건 아직 사용자에게 제안만 했고 확정된 요청은 아니다.
- **미해결 논의**: 사용자가 "`?demo=preview` 대신 배포 앱 순수 주소 자체에 데모 데이터를 기본으로 넣는 게 낫지 않냐"고 질문했다. 답변한 트레이드오프: 순수 주소는 `loadState()`가 브라우저에 영구 저장하는 방식이라 발표 중 실수해도 새로고침으로 리셋이 안 되고, 앱 원래 기본 데이터(한도담/박지연/에듀수학학원)를 영구히 덮어쓰게 된다. 사용자는 아직 최종 결정을 안 했다 — 다음 대화에서 이어질 수 있다.
- 새로 만질 때 주의: `createPresentationDemoState` 안의 `planFor(enrollment)`가 학원ID+반이름으로 수강료를 찾으므로, 반을 추가/변경할 때는 `tuitionPlans`에도 같은 학원ID+className 조합을 추가해야 한다. 청구서를 추가할 때는 `enrollments[i]`의 인덱스가 아니라 `studentId`/`academyId`를 해당 enrollment에서 직접 읽어와야 한다(전에 인덱스로만 매칭해서 학생이 잘못 연결되는 버그가 있었다).

### Firebase 캐시 버스팅

- `index.html`의 자산 쿼리 버전(`?v=...`)이 8/11, 8/14 날짜로 오래돼 있어, 오늘 배포한 CSS/JS 변경이 이미 사이트를 방문한 브라우저에는 최대 1시간(Firebase Hosting 기본 `max-age=3600`) 동안 반영되지 않는 문제를 발견해 고쳤다. `styles.css`/`payments.js`/`qa-data.js`/`app.js` 네 개 쿼리 버전을 모두 `20260818-01`로 통일했다.
- **앞으로 이 네 파일 중 하나라도 고치고 배포할 때마다 `index.html`의 해당 `?v=` 값을 새 날짜/번호로 반드시 올린다.** `index.html` 자체는 Firebase에서 `Cache-Control: no-cache`라 항상 새로 받아오지만, 쿼리 문자열이 그대로면 스크립트/CSS 파일은 캐시된 옛 버전을 계속 쓴다.

### 이전(8/14)에 되돌린/삭제한 것 — 참고용, 계속 유효함

- 학부모가 청구 건을 선택해 "고정 가상계좌로 입금" 요청하던 버튼/흐름, 입금 완료 수동 확인 버튼, 테스트 입금액 시뮬레이션 입력창 — 모두 삭제됨, 실제 KCP 웹훅 전까지 학부모 화면에서 입금을 재현할 UI가 없다.
- `payments.js`의 죽은 코드 `tuition-plan-form` 제출 핸들러, `STATUS_LABELS.awaiting_deposit`.

## 현재 구현 범위

- 원장: 자동 청구 기준, 반별 수강료, 청구·납부 내역(잔액 표시 포함), 미납 알림 발송
- 학부모: 자동결제/직접결제 설정(+ 카드 등록 버튼), 직접결제 표(원생·학원 필터 + 전체선택), 고정 가상계좌(총 입금예정액/입금액/미납액), 결제 내역(원생·학원·상태 필터 + 월별·연별)
- 운영자: 학원별 결제 운영 현황
- 가상계좌 자동 배분(FIFO·부분납부·이월·예치금), 결제 상태·알림·모의 카드 자동결제
- 대용량 QA 데이터(`?qa=large`, 로컬 전용) + 발표용 소규모 데모(`?demo=preview`, 배포 도메인 포함)
- 결제 전용 테스트: `tests/payment-flow.test.cjs` (13개, 모두 통과)
- `app.js`의 `renderView()`에 렌더링 오류 안전장치(화면이 비는 대신 오류를 표시)

주요 파일: 결제 로직 `payments.js`, 앱 연결 `app.js`, 스타일 `styles.css`, QA/데모 데이터 `qa-data.js`, 배포 자산 연결 `index.html`·`scripts/prepare-firebase-hosting.cjs`.

## 검증 명령어

```text
node --check app.js
node --check payments.js
node --check qa-data.js
npm test
npm run test:payments
git diff --check
```

`npm test`(core-flow)는 결제와 무관한 **기존 실패 11건**이 항상 있다(출결 선택 처리, 학원 기본정보 2항목 배치, 학습분석/성장추이 관련 4건, 모바일 가로 넘침, 학부모 다자녀 연결, 학부모 타임라인 이름 표시, 과제 예외). 2026-08-18 기준 정확히 39개 중 28 pass / 11 fail로 확인됨 — 이 숫자와 다르면 새로운 회귀가 생긴 것이니 원인을 찾는다. `npm run test:payments`는 13/13 통과해야 한다.

브라우저에서 직접 확인할 것: 일반 화면·`?qa=large`·`?demo=preview` 각각에서 4개 역할(원장/강사/학부모/운영자) 로그인, 결제 설정 폼(자동/직접 두 모드) 레이아웃, 학부모 결제 내역 필터, 데스크톱(1280px)·태블릿(761~1180px)·모바일(375px) 겹침/넘침 없는지.

## GitHub·Firebase 작업 절차 (참고용 — 이미 완료된 상태)

```text
git add <작업한 파일만 명시>   # moaflow_brand_update/, tools/ 는 절대 추가하지 않는다
git commit -m "..."
git push origin codex/tuition-payments   # 또는 새 기능 브랜치
git checkout main && git pull origin main --ff-only
git merge --no-ff codex/tuition-payments -m "..."
git push origin main
# index.html의 ?v= 버전을 먼저 올렸는지 확인한 뒤:
firebase deploy --only hosting
```

배포 후 확인: `curl -sI https://moaflow-1b9c8.web.app/qa-data.js`로 캐시 헤더 확인, 새 브라우저 탭(캐시 영향 없는)에서 실제 로그인·화면 확인.

## 계정과 보안

- GitHub/Firebase 토큰은 저장소나 커밋에 저장하지 않는다.
- 새 장치에서는 `gh auth login`, `firebase login`으로 각각 새로 인증한다(이전 장치 토큰은 옮겨지지 않음).
- 인증·권한이 없으면 사용자에게 로그인을 요청하고, 임의 계정으로 대체하지 않는다.
